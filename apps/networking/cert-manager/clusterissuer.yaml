apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: admin@chalk-ho.me
    privateKeySecretRef:
      name: letsencrypt
    solvers:
      - dns01:
          cloudflare:
            email: admin@chalk-playground.space

            apiTokenSecretRef:
              name: cert-manager-secret
              key: CLOUDFLARE_API_TOKEN
        selector:
          dnsZones:
            - chalk-ho.me

---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned
spec:
  selfSigned: {}
