apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bridges-postgres
  namespace: matrix
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-device
  resources:
    requests:
      storage: 15Gi
