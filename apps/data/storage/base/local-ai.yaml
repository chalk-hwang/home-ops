apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: localai-storage
  namespace: mlops
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: local-device
  resources:
    requests:
      storage: 100Gi
