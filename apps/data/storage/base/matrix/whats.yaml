apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: whats-bridge-data
  namespace: matrix1
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-device
  resources:
    requests:
      storage: 1Gi
