connect:
  api:
    httpPort: 80
    httpsPort: 443
  ingress:
    enabled: true
    ingressClassName: internal
    annotations:
      kubernetes.io/ingress.class: internal
    hosts:
      - host: &host "onepassword-connect.chalk-ho.me"
        paths: []
    tls:
      - hosts:
          - *host
