apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
# - nextdns.yamlargocd/base/networking/external-dns/kustomization.yaml
  - certificate.yaml
  - external.yaml
  - internal.yaml
namespace: argocd

commonLabels:
  app.kubernetes.io/project: ingress-nginx
